Add your static assets here.

Expected / commonly used files:

- `resume.pdf` — linked from the Hero “Download Résumé” button
- `matlum.png` — used in the About section (profile image)
- `favicon.ico` — browser tab icon

Optional:

- `og-image.png` — for social share preview images (referenced in `app/layout.tsx` metadata if you add it)

